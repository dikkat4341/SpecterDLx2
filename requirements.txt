# SpecterDLx2 requirements.txt - Portable Python IPTV Downloader
# .NET yok, %100 Python + PyInstaller için optimize
# Test edildi: Python 3.12+ (2026 stabil)

customtkinter>=5.2.2    # Modern dark GUI (main.py için KRİTİK)
requests>=2.32.3        # HTTP istekleri (Xtream API, m3u8 indirme)
m3u8>=4.0.0             # HLS m3u8 parse (segment indirme)
pycryptodome>=3.20.0    # HLS AES decrypt (güvenlikli şifre çözme)
orjson>=3.10.7          # Hızlı JSON (config/favoriler – orjson %30 daha hızlı)
tqdm>=4.66.5            # Progress bar (indirme hızı/ETA)
yt-dlp>=2025.12.1       # YouTube + 1000+ site (isteğe bağlı)
pillow>=10.4.0          # Icon/resim (GUI için opsiyonel)

# Portable .exe build için (yerel makinede):
# pip install -r requirements.txt
# pip install pyinstaller
# pyinstaller --onefile --add-binary binaries/ffmpeg.exe:. src/main.py
# ... önceki paketler ...
yt-dlp>=2025.10.1  # YouTube + 1000+ site indirme desteği
